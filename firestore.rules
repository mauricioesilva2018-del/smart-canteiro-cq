rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Proteção de dados: exige autenticação ativa via Firebase Authentication
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
